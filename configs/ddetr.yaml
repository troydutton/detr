defaults:
  - hydra: simple
  - dataset: coco
  - transforms: nano
  - model: ddetr
  - optimizer: ddetr 
  - scheduler: step
  - criterion: ddetr
  - ema: constant 
  - _self_

train:
  output_dir: /path/to/output/dir
  num_epochs: 50 
  batch_size: 4
  num_workers: 4
  save_period: 10
  max_grad_norm: 0.1
  grad_accum_steps: 1
  random_seed: 42
  checkpoint: Null
