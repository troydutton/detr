resolution: 640

train:
  _target_: data.make_transformations
  split: train
  resolution: ${transforms.resolution}

val:
  _target_: data.make_transformations
  split: val 
  resolution: ${transforms.resolution}
